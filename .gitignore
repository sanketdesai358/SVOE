# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Raw API cache (regeneratable, one CSV per team)
data/raw/

# Keep processed data and models so Streamlit Cloud can run without retraining
# data/processed/   ← intentionally NOT ignored
# models/           ← intentionally NOT ignored
models/calibration.png

# Streamlit
.streamlit/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
